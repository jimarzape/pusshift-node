# pusshift-node

## set up env file ##
HOST='localhost'
DB_USER='username'
PASSWORD='password'
DB='database'
PORT=3001

## migration using knex ##
run this command to migrate tables
`knex migrate:latest`
